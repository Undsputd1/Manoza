{{ form_start(form, {'attr': {'id': 'orderForm'}}) }}

    {# Render all form fields #}
    {{ form_widget(form) }}

    {# Submit Button with proper styling for dark theme #}
    <div class="mt-3">
        <button type="submit" class="btn btn-success text-white fw-bold">
            {{ button_label|default('Save') }}
        </button>
    </div>

{{ form_end(form) }}

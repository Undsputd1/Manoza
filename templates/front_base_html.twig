<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}AVENNTA{% endblock %}</title>

    {# Bootstrap CSS #}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    {# Custom CSS #}
    <link rel="stylesheet" href="{{ asset('css/front.css') }}">

    <style>
        /* Dark theme for the whole site */
        body {
            background-color: #121212; /* dark gray */
            color: #ffffff; /* default text color */
        }

        main.container {
            background-color: #121212; /* make main container dark too */
        }

        /* Optional: style links and buttons to match dark theme */
        a.nav-link {
            color: #ccc;
        }
        a.nav-link.active {
            color: #fff;
        }
    </style>
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container d-flex justify-content-between align-items-center">
            <a class="navbar-brand" href="{{ path('app_home') }}">AVENNTA</a>

            <div class="d-flex align-items-center gap-3">
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav me-auto">
                        <li class="nav-item">
                            <a class="nav-link {% if app.request.attributes.get('_route') == 'app_home' %}active{% endif %}" 
                               href="{{ path('app_home') }}">Home</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link {% if app.request.attributes.get('_route') starts with 'app_shop' %}active{% endif %}" 
                               href="{{ path('app_shop_index') }}">Shop</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="#">About</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="#">Contact</a>
                        </li>
                    </ul>
                </div>

                {# 🛠️ Switch to Admin Button #}
                <a href="{{ path('app_admin') }}" class="btn btn-outline-warning btn-sm">
                    🛠️ Admin Panel
                </a>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="container mt-4">
        {% block body %}{% endblock %}
    </main>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

{% extends 'base.html.twig' %}

{% block title %}Create New Order{% endblock %}

{% block body %}
<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <div class="card shadow-lg border-0 rounded-4">
        <div class="card-header bg-primary text-white text-center py-3">
          <h3 class="mb-0">🛒 Create New Order</h3>
        </div>
        <div class="card-body p-4">
          {{ form_start(form) }}
            {{ form_widget(form, {'attr': {'class': 'form-control'}}) }}
            <div class="mt-4 text-center">
              <button class="btn btn-success px-4 py-2">💾 Save Order</button>
              <a href="{{ path('app_order_index') }}" class="btn btn-secondary px-4 py-2">↩ Back to List</a>
            </div>
          {{ form_end(form) }}
        </div>
      </div>
    </div>
  </div>
</div>
{% endblock %}
